Terminator = ';';
ReplyTimeout = 2000;
ExtraInput = Error;

getMode {
    out "QISTATE?";
    in "%i";    
}

setMode {
    out "%{SMON|SCONT}";
	@init { getMode; }
}

getTemp {
	out "QSAMP?\$1";
	in "%f%(\$2)c";	
}

setTemp {
	out "SETP\$1,%f";
}

getSpRbv {
	out "QSETP?\$1";
	in "%f%(\$2)c";	
}
