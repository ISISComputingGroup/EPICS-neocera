Terminator = ';';
ReplyTimeout = 2000;
ExtraInput = Error;

getMode {
    out "QISTATE?";
    #Read no more that 39 chars (EPICS limit)
    in "%i";    
}

setMode {
    out "%{SMON|SCONT}";
	@init { getMode; }
}

getTemp1 {
	out "QSAMP?1";
	in "%f%(\$1)c";	
}
